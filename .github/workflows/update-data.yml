name: Update gold market data

on:
  workflow_dispatch:
  schedule:
    - cron: "15 7 * * 1-5"

permissions:
  contents: write

concurrency:
  group: update-gold-etf-data
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Refresh World Gold Council data
        run: python scripts/update_data.py

      - name: Refresh FRED real-yield data
        run: python scripts/update_real_yield.py

      - name: Refresh Yahoo Finance COMEX gold data
        run: python scripts/update_comex_gold.py

      - name: Refresh Yahoo Finance dollar-index data
        run: python scripts/update_dollar_index.py

      - name: Commit updated data
        shell: bash
        run: |
          if git diff --quiet -- data/gold_etf_flows_by_region.json data/gold_etf_flows_by_region.csv data/gold_price_by_frequency.csv data/gold_etf_holdings_by_region.json data/gold_etf_holdings_by_region.csv data/us_10y_real_yield.json data/us_10y_real_yield.csv data/comex_gold_futures.json data/comex_gold_futures.csv data/us_dollar_index.json data/us_dollar_index.csv; then
            echo "No source data change."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add data/gold_etf_flows_by_region.json data/gold_etf_flows_by_region.csv data/gold_price_by_frequency.csv data/gold_etf_holdings_by_region.json data/gold_etf_holdings_by_region.csv data/us_10y_real_yield.json data/us_10y_real_yield.csv data/comex_gold_futures.json data/comex_gold_futures.csv data/us_dollar_index.json data/us_dollar_index.csv
          git commit -m "Update gold market data"
          git push
